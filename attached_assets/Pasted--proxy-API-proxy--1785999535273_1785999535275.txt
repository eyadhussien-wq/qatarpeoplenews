سأضيف proxy فعلياً إلى خادم الـ API الحالي، ثم أربط مكوّن الراديو بمسار proxy داخلي مع مفاتيح المحطات v18، وأختبر TypeScript وتشغيل الخادمين. أولاً سأحدد بنية مسارات API الحالية حتى أضيف المسار دون كسر التسجيل القائم.


6 actions
سأفحص مسار الـ API الحالي وأضيف proxy تحت /api/radio-proxy/:station مع إعادة كتابة روابط الـ playlist النسبية، ثم أعدل نموذج المحطة ليحمل key ويشغل عنوان proxy من التطبيق.


4 actions
سأضيف مسار HLS proxy إلى خادم الـ API الحالي مع إعادة كتابة روابط الـ playlist، ثم أغيّر نموذج المحطة إلى key ومشغل Audio.Sound native يستخدم عنوان الـ API المناسب للويب والموبايل. بعد ذلك سأشغّل خادم API وExpo وأجري الفحوص.


4 actions
تمت إضافة مسار proxy وربط المشغل native بالمفاتيح v18. سأفحص TypeScript في خادم API والتطبيق، ثم أعيد تشغيل الخادمين وأختبر مسار /api/radio-proxy فعلياً قبل التحقق من المعاينة.


5 actions
نجحت فحوص TypeScript للخادم والتطبيق، وأعيد تشغيل الخادمين. سأختبر الآن نقطة proxy عبر المسار المحلي وأراجع السجلات والمعاينة النهائية للتأكد من عدم وجود أخطاء تشغيل.


4 actions
تم تشغيل الخادمَين وفحص الأنواع بنجاح. اختبار curl السابق استخدم المنفذ 3000 بينما خادم API يعمل على 8080؛ سأصحح الآن عنوان proxy في التطبيق ليستخدم نفس نطاق API المهيأ في المشروع، ثم أختبر المسار على 8080 وأعيد التحقق.


4 actions
اختبار الـ proxy وصل إلى خادم API فعلاً، لكنه أعاد خطأ upstream من qmcconnect.qa؛ كما لاحظت أن نسخة الويب كانت تستخدم مساراً نسبياً قد يذهب إلى خادم Expo بدلاً من API. سأوحّد عنوان الـ API في الويب والموبايل باستخدام EXPO_PUBLIC_DOMAIN ثم أعيد الفحص والتشغيل.


2 actions
تم توحيد عنوان الـ API للويب والموبايل باستخدام EXPO_PUBLIC_DOMAIN. سأجري الفحص النهائي وأعيد تشغيل Expo، ثم أتحقق من السجلات؛ وسأكون واضحاً إذا ظل upstream نفسه يمنع proxy بطبقة حماية إضافية.


5 actions
اكتملت إعادة التشغيل والفحوص الأخيرة. سأراجع السجل والمعاينة واختبار proxy النهائي للتأكد من أن التطبيق يستخدم عنوان API الصحيح، مع فصل أي مشكلة متبقية في upstream عن كود التطبيق.


5 actions
تم تنفيذ بنية الـ Backend Stream Proxy v18:

إضافة المسار:
/api/radio-proxy/:station

إضافة خريطة المحطات:
qur
skfm
qr
alrayyanfm
إرسال User-Agent وReferer وOrigin.
إعادة كتابة الروابط النسبية داخل ملفات HLS.
إضافة CORS headers وCache-Control: no-store.
تحديث RadioStation ليستخدم key.
تحديث الإصدار إلى:
v18_backend_proxy

تحويل RadioSection.tsx إلى expo-av وAudio.Sound native.
إضافة دعم:
التشغيل في الخلفية.
التشغيل أثناء الوضع الصامت على iOS.
pause/resume.
unload عند إغلاق المشغل.
حالات التحميل والخطأ.
توحيد عنوان API للويب والموبايل باستخدام EXPO_PUBLIC_DOMAIN.
AsyncStorage سيستبدل المحطات المحفوظة عند اختلاف إصدار v18.
نجح فحص TypeScript للتطبيق والخادم.
تم إعادة تشغيل Expo وخادم API بنجاح.
المعاينة تعمل دون أخطاء JavaScript أو أخطاء بناء.
ملاحظة مهمة: تم اختبار مسار proxy فعلياً، لكنه ما زال يعيد:

Stream upstream error

من qmcconnect.qa. هذا يعني أن الخادم upstream يمنع طلبات الخادم أيضاً، وليس مجرد مشكلة CORS في المتصفح. لذلك تم تجهيز المسار والعميل بشكل صحيح، لكن لا يمكنني الادعاء بأن الصوت يعمل حالياً حتى يسمح upstream بالطلبات أو يتم توفير endpoint/token صالح للبث عبر الخادم.


Worked for 2 minutes

Checkpoint made
just now

Scroll to latest

Message Agent...





Qatar News Portal - Replit